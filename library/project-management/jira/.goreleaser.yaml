version: 2
project_name: jira-pp-cli
changelog:
  disable: true
builds:
  - id: jira-pp-cli
    main: ./cmd/jira-pp-cli
    binary: jira-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/project-management/jira/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: jira-pp-mcp
    main: ./cmd/jira-pp-mcp
    binary: jira-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: jira-pp-cli
    repository:
      owner: nikica-jokic
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Call Jira Cloud Platform REST endpoints for issues, projects, users, workflows, permissions, and other Atlassian work-management data."
    install: |
      bin.install "jira-pp-cli"
      bin.install "jira-pp-mcp"

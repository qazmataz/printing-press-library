// Copyright 2026 Hamza Qazi and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTopsGetTrendingCmd(flags *rootFlags) *cobra.Command {
	var flagStore int
	var flagType string
	var flagCountry string
	var flagDateFrom string
	var flagDateTo string
	var flagLimit int
	var flagDepth int
	var flagMethod string
	var flagTagIds string
	var flagCategoryIds string
	var flagHeadquarter string
	var flagNewOnly bool
	var flagKind string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-trending",
		Short:       "Retrieves a list of apps that have ranked up in the selected top chart for the selected country (or WW)",
		Example:     "  appmagic-pp-cli tops get-trending --store 1 --type top_free --country WW --date-from 2026-01-15 --date-to 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "tops.get-trending", "pp:method": "GET", "pp:path": "/tops/trending", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("store") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "store")
			}
			if !cmd.Flags().Changed("type") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "type")
			}
			if !cmd.Flags().Changed("country") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "country")
			}
			if !cmd.Flags().Changed("date-from") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "date-from")
			}
			if !cmd.Flags().Changed("date-to") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "date-to")
			}
			if cmd.Flags().Changed("type") {
				allowedType := []string{"top_free", "top_grossing"}
				validType := false
				for _, v := range allowedType {
					if flagType == v {
						validType = true
						break
					}
				}
				if !validType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagType, "type", allowedType)
				}
			}
			if cmd.Flags().Changed("country") {
				allowedCountry := []string{"WW", "W1", "E1", "US", "GB", "AU", "CA", "DE", "FR", "NL", "CH", "SE", "PL", "BE", "NO", "AT", "DK", "IE", "RU", "NG", "ZA", "BR", "MX", "AR", "IT", "ES", "CO", "JP", "CN", "HK", "KR", "IN", "ID", "TW", "TH", "MY", "SG", "PH", "TR", "SA", "IR", "AE", "EG", "IL", "VN", "NZ", "FI", "PK", "CL", "PT", "PE", "KW", "EC", "UA", "RO", "DZ", "HU", "DO", "BY", "AZ", "KZ", "CZ", "LB", "GR", "BD", "BH", "GE", "IQ", "JO", "KH", "MA", "MM", "MO", "OM", "QA", "UY", "UZ"}
				validCountry := false
				for _, v := range allowedCountry {
					if flagCountry == v {
						validCountry = true
						break
					}
				}
				if !validCountry {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagCountry, "country", allowedCountry)
				}
			}
			if cmd.Flags().Changed("method") {
				allowedMethod := []string{"gainers", "losers"}
				validMethod := false
				for _, v := range allowedMethod {
					if flagMethod == v {
						validMethod = true
						break
					}
				}
				if !validMethod {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagMethod, "method", allowedMethod)
				}
			}
			if cmd.Flags().Changed("kind") {
				allowedKind := []string{"all", "games", "apps"}
				validKind := false
				for _, v := range allowedKind {
					if flagKind == v {
						validKind = true
						break
					}
				}
				if !validKind {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagKind, "kind", allowedKind)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/tops/trending"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "tops", path, map[string]string{
				"store":        formatCLIParamValue(flagStore),
				"type":         formatCLIParamValue(flagType),
				"country":      formatCLIParamValue(flagCountry),
				"date_from":    formatCLIParamValue(flagDateFrom),
				"date_to":      formatCLIParamValue(flagDateTo),
				"limit":        formatCLIParamValue(flagLimit),
				"depth":        formatCLIParamValue(flagDepth),
				"method":       formatCLIParamValue(flagMethod),
				"tag_ids":      formatCLIParamValue(flagTagIds),
				"category_ids": formatCLIParamValue(flagCategoryIds),
				"headquarter":  formatCLIParamValue(flagHeadquarter),
				"new_only":     formatCLIParamValue(flagNewOnly),
				"kind":         formatCLIParamValue(flagKind),
			}, nil, flagAll, "", "offset", "limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagStore, "store", 0, "1 - Google Play Market 2 - iPhone App Store 3 - iPad App Store 4 - iPhone + iPad App Store 5 - All Stores (one of: 1, 2, 3, 4, 5)")
	cmd.Flags().StringVar(&flagType, "type", "", "Type of a top (one of: top_free, top_grossing)")
	cmd.Flags().StringVar(&flagCountry, "country", "", "See [available countries](#tag/available-countries) (one of: WW, W1, E1, US, GB, AU, CA, DE, FR, NL, CH, SE, PL, BE, NO, AT, DK, IE, RU, NG, ZA, BR, MX, AR, IT, ES, CO, JP, CN, HK, KR, IN, ID, TW, TH, MY, SG, PH, TR, SA, IR, AE, EG, IL, VN, NZ, FI, PK, CL, PT, PE, KW, EC, UA, RO, DZ, HU, DO, BY, AZ, KZ, CZ, LB, GR, BD, BH, GE, IQ, JO, KH, MA, MM, MO, OM, QA, UY, UZ)")
	cmd.Flags().StringVar(&flagDateFrom, "date-from", "", "Minimum date is 2015-01-01")
	cmd.Flags().StringVar(&flagDateTo, "date-to", "", "Minimum date is 2015-01-01")
	cmd.Flags().IntVar(&flagLimit, "limit", 100, "The number of items returned.")
	cmd.Flags().IntVar(&flagDepth, "depth", 10000, "Top app analysis limit.")
	cmd.Flags().StringVar(&flagMethod, "method", "gainers", "Type of a top (one of: gainers, losers)")
	cmd.Flags().StringVar(&flagTagIds, "tag-ids", "", "Tag ids")
	cmd.Flags().StringVar(&flagCategoryIds, "category-ids", "", "See [categories section](#tag/categories)")
	cmd.Flags().StringVar(&flagHeadquarter, "headquarter", "", "Alpha-2 country code")
	cmd.Flags().BoolVar(&flagNewOnly, "new-only", false, "Apps that were either released in the specified time period in the selected country and/or apps that had not been")
	cmd.Flags().StringVar(&flagKind, "kind", "all", "Show All, games only or apps only (one of: all, games, apps)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}

// Copyright 2026 Hamza Qazi and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAdintGetApplicationAdsCmd(flags *rootFlags) *cobra.Command {
	var flagAppIds string
	var flagCountry string
	var flagDateFrom string
	var flagDateTo string
	var flagLoadLastSeen bool
	var flagSort string
	var flagAggregation string
	var flagCount int
	var flagOffset string
	var flagAdNetwork string
	var flagAdSource string
	var flagPlatform string
	var flagType string
	var flagTagId string
	var flagCreatedDateFrom string
	var flagCreatedDateTo string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-application-ads",
		Short:       "Retrieves links to ad creatives for the selected app over the selected period of time.",
		Example:     "  appmagic-pp-cli adint get-application-ads --country WW --date-from 2026-01-15 --date-to 2026-01-15 --sort score --aggregation day",
		Annotations: map[string]string{"pp:endpoint": "adint.get-application-ads", "pp:method": "GET", "pp:path": "/adint/application-ads", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("country") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "country")
			}
			if !cmd.Flags().Changed("date-from") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "date-from")
			}
			if !cmd.Flags().Changed("date-to") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "date-to")
			}
			if !cmd.Flags().Changed("sort") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "sort")
			}
			if !cmd.Flags().Changed("aggregation") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "aggregation")
			}
			if cmd.Flags().Changed("country") {
				allowedCountry := []string{"WW", "W1", "E1", "US", "GB", "AU", "CA", "DE", "FR", "NL", "CH", "SE", "PL", "BE", "NO", "AT", "DK", "IE", "RU", "NG", "ZA", "BR", "MX", "AR", "IT", "ES", "CO", "JP", "CN", "HK", "KR", "IN", "ID", "TW", "TH", "MY", "SG", "PH", "TR", "SA", "IR", "AE", "EG", "IL", "VN", "NZ", "FI", "PK", "CL", "PT", "PE", "KW", "EC", "UA", "RO", "DZ", "HU", "DO", "BY", "AZ", "KZ", "CZ", "LB", "GR", "BD", "BH", "GE", "IQ", "JO", "KH", "MA", "MM", "MO", "OM", "QA", "UY", "UZ"}
				validCountry := false
				for _, v := range allowedCountry {
					if flagCountry == v {
						validCountry = true
						break
					}
				}
				if !validCountry {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagCountry, "country", allowedCountry)
				}
			}
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{"score", "created_date"}
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSort, "sort", allowedSort)
				}
			}
			if cmd.Flags().Changed("aggregation") {
				allowedAggregation := []string{"day", "week", "month"}
				validAggregation := false
				for _, v := range allowedAggregation {
					if flagAggregation == v {
						validAggregation = true
						break
					}
				}
				if !validAggregation {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAggregation, "aggregation", allowedAggregation)
				}
			}
			if cmd.Flags().Changed("ad-network") {
				allowedAdNetwork := []string{"adcolony", "admob", "applovin", "chartboost", "facebook", "fyber", "inmobi", "ironsource", "mopub", "pangle", "tapjoy", "tiktok", "unity", "vungle", "youtube"}
				validAdNetwork := false
				for _, v := range allowedAdNetwork {
					if flagAdNetwork == v {
						validAdNetwork = true
						break
					}
				}
				if !validAdNetwork {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAdNetwork, "ad-network", allowedAdNetwork)
				}
			}
			if cmd.Flags().Changed("platform") {
				allowedPlatform := []string{"android", "ios"}
				validPlatform := false
				for _, v := range allowedPlatform {
					if flagPlatform == v {
						validPlatform = true
						break
					}
				}
				if !validPlatform {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagPlatform, "platform", allowedPlatform)
				}
			}
			if cmd.Flags().Changed("type") {
				allowedType := []string{"video", "interstitialVideo", "rewardedVideo", "playable", "html", "static", "web2app"}
				validType := false
				for _, v := range allowedType {
					if flagType == v {
						validType = true
						break
					}
				}
				if !validType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagType, "type", allowedType)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/adint/application-ads"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "adint", path, map[string]string{
				"appIds":          formatCLIParamValue(flagAppIds),
				"country":         formatCLIParamValue(flagCountry),
				"dateFrom":        formatCLIParamValue(flagDateFrom),
				"dateTo":          formatCLIParamValue(flagDateTo),
				"loadLastSeen":    formatCLIParamValue(flagLoadLastSeen),
				"sort":            formatCLIParamValue(flagSort),
				"aggregation":     formatCLIParamValue(flagAggregation),
				"count":           formatCLIParamValue(flagCount),
				"offset":          formatCLIParamValue(flagOffset),
				"adNetwork":       formatCLIParamValue(flagAdNetwork),
				"adSource":        formatCLIParamValue(flagAdSource),
				"platform":        formatCLIParamValue(flagPlatform),
				"type":            formatCLIParamValue(flagType),
				"tagId":           formatCLIParamValue(flagTagId),
				"createdDateFrom": formatCLIParamValue(flagCreatedDateFrom),
				"createdDateTo":   formatCLIParamValue(flagCreatedDateTo),
			}, nil, flagAll, "offset", "offset", "", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAppIds, "app-ids", "", "Application id in store, for example `com.helixload.syxme.vkmp`")
	cmd.Flags().StringVar(&flagCountry, "country", "", "See [available countries](#tag/available-countries) (one of: WW, W1, E1, US, GB, AU, CA, DE, FR, NL, CH, SE, PL, BE, NO, AT, DK, IE, RU, NG, ZA, BR, MX, AR, IT, ES, CO, JP, CN, HK, KR, IN, ID, TW, TH, MY, SG, PH, TR, SA, IR, AE, EG, IL, VN, NZ, FI, PK, CL, PT, PE, KW, EC, UA, RO, DZ, HU, DO, BY, AZ, KZ, CZ, LB, GR, BD, BH, GE, IQ, JO, KH, MA, MM, MO, OM, QA, UY, UZ)")
	cmd.Flags().StringVar(&flagDateFrom, "date-from", "", "Minimum date is 2015-01-01")
	cmd.Flags().StringVar(&flagDateTo, "date-to", "", "Minimum date is 2015-01-01")
	cmd.Flags().BoolVar(&flagLoadLastSeen, "load-last-seen", false, "Load last seen")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort (one of: score, created_date)")
	cmd.Flags().StringVar(&flagAggregation, "aggregation", "", "Creatives will be summarazed in given period (one of: day, week, month)")
	cmd.Flags().IntVar(&flagCount, "count", 100, "The number of items returned.")
	cmd.Flags().StringVar(&flagOffset, "offset", "0", "Offset is the position in the dataset of a particular record.")
	cmd.Flags().StringVar(&flagAdNetwork, "ad-network", "", "Ad network (one of: adcolony, admob, applovin, chartboost, facebook, fyber, inmobi, ironsource, mopub, pangle, tapjoy, tiktok, unity, vungle, youtube)")
	cmd.Flags().StringVar(&flagAdSource, "ad-source", "", "For the list of available values see /adint/filter_values")
	cmd.Flags().StringVar(&flagPlatform, "platform", "", "Application platform (one of: android, ios)")
	cmd.Flags().StringVar(&flagType, "type", "", "Type (one of: video, interstitialVideo, rewardedVideo, playable, html, static, web2app)")
	cmd.Flags().StringVar(&flagTagId, "tag-id", "", "Tag id")
	cmd.Flags().StringVar(&flagCreatedDateFrom, "created-date-from", "", "Created date from")
	cmd.Flags().StringVar(&flagCreatedDateTo, "created-date-to", "", "Created date to")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}

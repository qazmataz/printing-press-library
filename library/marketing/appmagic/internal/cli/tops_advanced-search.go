// Copyright 2026 Hamza Qazi and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTopsAdvancedSearchCmd(flags *rootFlags) *cobra.Command {
	var flagReleaseDateGte string
	var flagReleaseDateLte string
	var flagCountry string
	var flagStore int
	var flagSize int
	var flagOffset string
	var flagSearchOperator string
	var flagScreenshots bool
	var flagSearch string
	var flagDescription string
	var flagRevenueFrom int
	var flagRevenueTo int
	var flagDownloadsFrom int
	var flagDownloadsTo int
	var flagSort string
	var flagSortAsc bool
	var flagHasWebShop bool
	var flagPaid bool
	var flagPreOrder bool
	var flagContainsAds bool
	var flagHasInAppPurchases bool
	var flagRemoved bool
	var flagCategoryIds string
	var flagTagIds string
	var flagPublisherPlace int
	var flagPublisherHq string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "advanced-search",
		Aliases:     []string{"list"},
		Short:       "Retrieves search results by multiple criteria, including keywords in both app names and descriptions",
		Example:     "  appmagic-pp-cli tops advanced-search --release-date-gte 2026-01-15 --release-date-lte 2026-01-15 --country WW --store 1 --size 50 --sort score",
		Annotations: map[string]string{"pp:endpoint": "tops.advanced-search", "pp:method": "GET", "pp:path": "/tops/advanced-search", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("release-date-gte") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "release-date-gte")
			}
			if !cmd.Flags().Changed("release-date-lte") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "release-date-lte")
			}
			if !cmd.Flags().Changed("country") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "country")
			}
			if !cmd.Flags().Changed("store") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "store")
			}
			if !cmd.Flags().Changed("size") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "size")
			}
			if !cmd.Flags().Changed("sort") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "sort")
			}
			if cmd.Flags().Changed("country") {
				allowedCountry := []string{"WW", "W1", "E1", "US", "GB", "AU", "CA", "DE", "FR", "NL", "CH", "SE", "PL", "BE", "NO", "AT", "DK", "IE", "RU", "NG", "ZA", "BR", "MX", "AR", "IT", "ES", "CO", "JP", "CN", "HK", "KR", "IN", "ID", "TW", "TH", "MY", "SG", "PH", "TR", "SA", "IR", "AE", "EG", "IL", "VN", "NZ", "FI", "PK", "CL", "PT", "PE", "KW", "EC", "UA", "RO", "DZ", "HU", "DO", "BY", "AZ", "KZ", "CZ", "LB", "GR", "BD", "BH", "GE", "IQ", "JO", "KH", "MA", "MM", "MO", "OM", "QA", "UY", "UZ"}
				validCountry := false
				for _, v := range allowedCountry {
					if flagCountry == v {
						validCountry = true
						break
					}
				}
				if !validCountry {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagCountry, "country", allowedCountry)
				}
			}
			if cmd.Flags().Changed("search-operator") {
				allowedSearchOperator := []string{"OR", "AND"}
				validSearchOperator := false
				for _, v := range allowedSearchOperator {
					if flagSearchOperator == v {
						validSearchOperator = true
						break
					}
				}
				if !validSearchOperator {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSearchOperator, "search-operator", allowedSearchOperator)
				}
			}
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{"score", "ltv", "revenue", "downloads", "dataCountries", "releaseDate"}
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSort, "sort", allowedSort)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/tops/advanced-search"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "tops", path, map[string]string{
				"release_date_gte":     formatCLIParamValue(flagReleaseDateGte),
				"release_date_lte":     formatCLIParamValue(flagReleaseDateLte),
				"country":              formatCLIParamValue(flagCountry),
				"store":                formatCLIParamValue(flagStore),
				"size":                 formatCLIParamValue(flagSize),
				"offset":               formatCLIParamValue(flagOffset),
				"search_operator":      formatCLIParamValue(flagSearchOperator),
				"screenshots":          formatCLIParamValue(flagScreenshots),
				"search":               formatCLIParamValue(flagSearch),
				"description":          formatCLIParamValue(flagDescription),
				"revenue_from":         formatCLIParamValue(flagRevenueFrom),
				"revenue_to":           formatCLIParamValue(flagRevenueTo),
				"downloads_from":       formatCLIParamValue(flagDownloadsFrom),
				"downloads_to":         formatCLIParamValue(flagDownloadsTo),
				"sort":                 formatCLIParamValue(flagSort),
				"sort_asc":             formatCLIParamValue(flagSortAsc),
				"has_web_shop":         formatCLIParamValue(flagHasWebShop),
				"paid":                 formatCLIParamValue(flagPaid),
				"pre_order":            formatCLIParamValue(flagPreOrder),
				"contains_ads":         formatCLIParamValue(flagContainsAds),
				"has_in_app_purchases": formatCLIParamValue(flagHasInAppPurchases),
				"removed":              formatCLIParamValue(flagRemoved),
				"category_ids":         formatCLIParamValue(flagCategoryIds),
				"tag_ids":              formatCLIParamValue(flagTagIds),
				"publisherPlace":       formatCLIParamValue(flagPublisherPlace),
				"publisher_hq":         formatCLIParamValue(flagPublisherHq),
			}, nil, flagAll, "offset", "offset", "", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagReleaseDateGte, "release-date-gte", "", "Minimum date is 2008-01-01")
	cmd.Flags().StringVar(&flagReleaseDateLte, "release-date-lte", "", "Minimum date is 2008-01-01")
	cmd.Flags().StringVar(&flagCountry, "country", "", "See [available countries](#tag/available-countries) (one of: WW, W1, E1, US, GB, AU, CA, DE, FR, NL, CH, SE, PL, BE, NO, AT, DK, IE, RU, NG, ZA, BR, MX, AR, IT, ES, CO, JP, CN, HK, KR, IN, ID, TW, TH, MY, SG, PH, TR, SA, IR, AE, EG, IL, VN, NZ, FI, PK, CL, PT, PE, KW, EC, UA, RO, DZ, HU, DO, BY, AZ, KZ, CZ, LB, GR, BD, BH, GE, IQ, JO, KH, MA, MM, MO, OM, QA, UY, UZ)")
	cmd.Flags().IntVar(&flagStore, "store", 0, "1 - Google Play Market 2 - iPhone App Store 3 - iPad App Store 4 - iPhone + iPad App Store 5 - All Stores (one of: 1, 2, 3, 4, 5)")
	cmd.Flags().IntVar(&flagSize, "size", 0, "The number of items returned.")
	cmd.Flags().StringVar(&flagOffset, "offset", "0", "Offset is the position in the dataset of a particular record.")
	cmd.Flags().StringVar(&flagSearchOperator, "search-operator", "OR", "Operator, how to match app name and description when searching (one of: OR, AND)")
	cmd.Flags().BoolVar(&flagScreenshots, "screenshots", false, "Screenshots")
	cmd.Flags().StringVar(&flagSearch, "search", "", "Use query building commands as conjunctions to combine or exclude keywords in a search")
	cmd.Flags().StringVar(&flagDescription, "description", "", "Use query building commands as conjunctions to combine or exclude keywords in a search")
	cmd.Flags().IntVar(&flagRevenueFrom, "revenue-from", 0, "range revenue from")
	cmd.Flags().IntVar(&flagRevenueTo, "revenue-to", 0, "range revenue to")
	cmd.Flags().IntVar(&flagDownloadsFrom, "downloads-from", 0, "range downloads from")
	cmd.Flags().IntVar(&flagDownloadsTo, "downloads-to", 0, "range downloads to")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Type of a top (one of: score, ltv, revenue, downloads, dataCountries, releaseDate)")
	cmd.Flags().BoolVar(&flagSortAsc, "sort-asc", false, "Sort asc")
	cmd.Flags().BoolVar(&flagHasWebShop, "has-web-shop", false, "Has web shop")
	cmd.Flags().BoolVar(&flagPaid, "paid", false, "Paid")
	cmd.Flags().BoolVar(&flagPreOrder, "pre-order", false, "Pre order")
	cmd.Flags().BoolVar(&flagContainsAds, "contains-ads", false, "Contains ads")
	cmd.Flags().BoolVar(&flagHasInAppPurchases, "has-in-app-purchases", false, "Has in app purchases")
	cmd.Flags().BoolVar(&flagRemoved, "removed", false, "Filter by removed from store status. True returns only apps removed from store, false returns only active apps.")
	cmd.Flags().StringVar(&flagCategoryIds, "category-ids", "", "See [categories section](#tag/categories)")
	cmd.Flags().StringVar(&flagTagIds, "tag-ids", "", "Tag ids")
	cmd.Flags().IntVar(&flagPublisherPlace, "publisher-place", 0, "Topmost publishers based on the sum of revenue and downloads in the last 30 days. (one of: 100, 1000, 5000)")
	cmd.Flags().StringVar(&flagPublisherHq, "publisher-hq", "", "Publisher hq")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}

// Copyright 2026 Hamza Qazi and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTopsGetPublishersCmd(flags *rootFlags) *cobra.Command {
	var flagDate string
	var flagStore int
	var flagAggregation string
	var flagTagIds string
	var flagHeadquarters string
	var flagCountries string
	var flagLimit int
	var flagSort string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-publishers",
		Short:       "Retrieves data on any publisher Top Chart (top free, top grossing, top featuring) for any country/region and any date.",
		Example:     "  appmagic-pp-cli tops get-publishers --date 2026-01-15 --store 1 --aggregation daily --countries example-value --sort top_free",
		Annotations: map[string]string{"pp:endpoint": "tops.get-publishers", "pp:method": "GET", "pp:path": "/tops/publishers", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "date")
			}
			if !cmd.Flags().Changed("store") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "store")
			}
			if !cmd.Flags().Changed("aggregation") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "aggregation")
			}
			if !cmd.Flags().Changed("countries") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "countries")
			}
			if !cmd.Flags().Changed("sort") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "sort")
			}
			if cmd.Flags().Changed("aggregation") {
				allowedAggregation := []string{"daily", "weekly", "monthly", "yearly"}
				validAggregation := false
				for _, v := range allowedAggregation {
					if flagAggregation == v {
						validAggregation = true
						break
					}
				}
				if !validAggregation {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAggregation, "aggregation", allowedAggregation)
				}
			}
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{"top_free", "top_grossing", "top_featuring"}
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSort, "sort", allowedSort)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/tops/publishers"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "tops", path, map[string]string{
				"date":         formatCLIParamValue(flagDate),
				"store":        formatCLIParamValue(flagStore),
				"aggregation":  formatCLIParamValue(flagAggregation),
				"tag_ids":      formatCLIParamValue(flagTagIds),
				"headquarters": formatCLIParamValue(flagHeadquarters),
				"countries":    formatCLIParamValue(flagCountries),
				"limit":        formatCLIParamValue(flagLimit),
				"sort":         formatCLIParamValue(flagSort),
			}, nil, flagAll, "", "offset", "limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDate, "date", "", "Minimum date is 2015-01-01")
	cmd.Flags().IntVar(&flagStore, "store", 0, "1 - Google Play Market 2 - iPhone App Store 3 - iPad App Store 4 - iPhone + iPad App Store 5 - All Stores (one of: 1, 2, 3, 4, 5)")
	cmd.Flags().StringVar(&flagAggregation, "aggregation", "", "Data will be summarized in given period (one of: daily, weekly, monthly, yearly)")
	cmd.Flags().StringVar(&flagTagIds, "tag-ids", "", "Tag ids")
	cmd.Flags().StringVar(&flagHeadquarters, "headquarters", "", "Headquarters")
	cmd.Flags().StringVar(&flagCountries, "countries", "", "Countries")
	cmd.Flags().IntVar(&flagLimit, "limit", 100, "The number of items returned.")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Type of a top (one of: top_free, top_grossing, top_featuring)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
